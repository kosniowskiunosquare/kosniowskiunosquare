// Ejemplos de funciones de la tabla de la tabla de la tabla

var nameImprover = function (name, adj) {
  return "col" + name + "mc" + adj + "pants";
};

// ejemplo 2

$("body").hide(); // hide es una funcion en este caso de la tabla

// ejemplo 3
myArray.forEach(function (item) {
  console.log(val);
});

// ejemplo 4

$("buttom").on("click", function () {
  console.log("dont press my buttons"); //.log  es un metodo
});
