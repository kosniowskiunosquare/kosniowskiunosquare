//  example of coercion

var numStudents = 16;

console.log("there are ${numStudents} students in the room.");

//  esto es un ejemplo de coercion al agregar un string y un numero
// automaticamente convierte el numero en string

// ejemplo al contrario

function addStudent(numStudents) {
  return numStudents + 1;
}

addStudent(+studentsInputElem.value);

// coercion to bolean

if (studentsInputElem.value) {
  numStudents = Number(studentsInputElem.value);
}

while (newStudents.length > 0) {
  enroolStudent = newStudents.pop();
}
